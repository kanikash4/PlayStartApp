# hello-playFramework

## Steps to run on locathost:  ##
 1. Create the template : 
     ```sh
    sbt new playframework/play-java-seed.g8
    ```
 2. Run the service : 
     ```sh
    sbt run
    ```
